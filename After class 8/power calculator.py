# Power Calculator

# Input the base number and the exponent
base = float(input("Enter the base number: "))
n = float(input("Enter the power (n): "))

# Calculate the result
result = base ** n

# Output the result
print(f"{base} raised to the power of {n} is: {result}")